# Serverless Node.js with Mocha/Chai and Typescript

Install:

```bash
npm install
```

Run tests:

```bash
npm test
```

Run locally:

```bash
sls invoke local -f hello
```

Deploy:

```bash
sls deploy
```