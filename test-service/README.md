# Serverless Node.js with Mocha/Chai and Typescript

Install:

```bash
yarn
```

Run tests:

```bash
yarn test
```

Run locally:

```bash
yarn run:local #runs `sls invoke local -f hello`
```

Deploy:

```bash
sls deploy
```
